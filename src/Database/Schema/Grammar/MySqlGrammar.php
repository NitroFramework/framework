<?php

namespace Nitro\Database\Schema\Grammar;

/**
 * MySQL and MariaDB schema grammar.
 *
 * The base grammar already speaks MySQL, so this exists to be named in a
 * driver map rather than to change anything.
 */
class MySqlGrammar extends Grammar
{
}
