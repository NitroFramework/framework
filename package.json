{
  "name": "nitro-framework-js",
  "description": "Build pipeline for the framework's bundled runtimes (Livewire + HTMX + NProgress glue).",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
